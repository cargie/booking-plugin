# ===================================
#  List Column Definitions
# ===================================

columns:
    status:
        label: Status
        searchable: true
        type: ucfirst
    id:
        label: ID
        searchable: true
    room:
        label: Room
        relation: room
        select: name
    customer:
        label: Customer
        relation: customer
        select: concat(name,' ',surname)
    booking_dates:
        label: Dates
        type: text