fields:
    pivot[rate]:
        label: Rate
        type: number
        default: 100
    pivot[type]:
        label: Rate Settings
        type: dropdown
        options:
            P: % from the cost per day
            F: per day